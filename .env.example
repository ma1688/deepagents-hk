# HKEX Agent 环境变量配置模板
# 复制此文件为 .env 并填入你的 API 密钥

# =============================================================================
# LLM 模型配置 (至少需要配置一个)
# =============================================================================

# 硅基流动 (SiliconFlow) - 推荐，优先级最高
# 获取 API 密钥: https://siliconflow.cn
SILICONFLOW_API_KEY=your_siliconflow_api_key_here
SILICONFLOW_MODEL=deepseek-chat  # 可选，默认: deepseek-chat

# OpenAI
# 获取 API 密钥: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-5-mini  # 可选，默认: gpt-5-mini

# Anthropic (Claude)
# 获取 API 密钥: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929  # 可选，默认: claude-sonnet-4-5-20250929

# =============================================================================
# 优先级说明
# =============================================================================
# 模型选择优先级: SiliconFlow > OpenAI > Anthropic
# 如果同时配置了多个，将按优先级顺序选择第一个可用的

# =============================================================================
# 使用说明
# =============================================================================
# 1. 复制此文件为 .env:
#    cp .env.example .env
#
# 2. 编辑 .env 文件，填入你的 API 密钥
#
# 3. 至少需要配置一个模型供应商的 API 密钥
#
# 4. .env 文件已添加到 .gitignore，不会被提交到版本控制
